version: 2.1
jobs:
  first_job:
    steps:
      - checkout
      - run:
          name: install library
          command: npm install
      - run:
          name: Build Image
          command: |
            npm docker build -t kt2505/third-image:latest .
      - run:
          name: Deploy Image to Registry
          command: |
            npm docker push kt2505/third-image:latest

workflows:
  version: 2
  run_project:
    jobs:
      - first_job
